-- 문제 : 동물 이름 중 두 번 이상 쓰인 이름과 해당 이름이 쓰인 횟수를 조회
-- 컬럼 : NAME, COUNT
-- 테이블 : ANIMAL_INS
-- 조건 : COUNT >= 2
-- 정렬 : 이름순
SELECT
    NAME,
    COUNT(*) AS COUNT
FROM ANIMAL_INS
WHERE NAME IS NOT NULL
GROUP BY NAME
HAVING COUNT >= 2
ORDER BY NAME